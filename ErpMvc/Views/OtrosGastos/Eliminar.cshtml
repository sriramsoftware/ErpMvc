@model ErpMvc.Models.OtrosGastos

@{
    ViewBag.Title = "Eliminar";
}
<div class="panel panel-primary">
    <div class="panel-heading">
        <h2 class="text-center">@ViewBag.Title</h2>

    </div>

    <div class="panel-body ">
        <h3 class="text-center">Esta seguro que desea borrar este registro?</h3>
        <div class="">
            <dl class="dl-horizontal col-md-offset-5">
                <dt>
                    @Html.DisplayNameFor(model => model.DiaContable)
                </dt>

                <dd>
                    @Model.DiaContable.Fecha.ToShortDateString()
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.ConceptoDeGasto)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.ConceptoDeGasto.Nombre)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Importe)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Importe)
                </dd>
            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color col-md-offset-8 col-md-4">
                    <input type="submit" value="Borrar" class="btn btn-danger" /> |
                    @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-default" })
                </div>
            }
        </div>
    </div>
</div>
